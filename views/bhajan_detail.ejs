<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= bhajan.title %></title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* Additional styling for bhajan details page */
        
        /* Center the content in the page */
        .bhajan-details-container {
            width: 65%;
            margin: 10px auto;
            padding: 20px;
            background-color: #F4E4C1;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        /* Title Styling */
        .bhajan-title {
            text-align: center;
            font-size: 2em;
            font-family: 'Georgia', serif;
            color: black; /* Rich brown color */
            margin-bottom: 20px;
        }

        /* Bhajan Text Styling */
        .bhajan-text {
            font-size: 22px;
            line-height: 1.6;
            color: #333; /* Darker font color for readability */
            background-color: #F4E4C1; /* White background for contrast */
            padding: 20px;
            border-radius: 5px;
            border: 1px solid #ddd;
        }

        .bhajan-text pre {
            white-space: pre-wrap; /* Ensure long text wraps within the container */
            word-wrap: break-word;
        }

        /* Ensure responsiveness */
        @media (max-width: 768px) {
            .bhajan-title {
                font-size: 1.5em;
            }

            .bhajan-text {
                font-size: 1em;
            }

            .bhajan-details-container {
            width: 85%;
           
        }

        }
    </style>
</head>
<body>

    <!-- Header -->
    <%- include('partials/header') %>
    <!-- Bhajan Details -->
    <main>
        <div class="bhajan-details-container">
            <!-- Title at the top and centered -->
            <h2 class="bhajan-title"><%= bhajan.title %></h2>

            <!-- Bhajan text with proper formatting -->
            <div class="bhajan-text">
                <pre><%- bhajan.bhajan %></pre>
            </div>
        </div>


            
    </main>

      <!-- Footer -->
        <%- include('partials/footer') %>

   

</body>
</html>